version: 5

defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: "Per-node data"
    path: "%{data_source}.yaml"

  - name: "Other YAML hierarchy levels"
    paths:
      - "global.yaml"
      - "defaults.yaml"
