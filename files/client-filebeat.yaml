classes:
  - filebeat

filebeat::prospector:
  syslogs:
    paths:
      - '/var/log/auth.log',
      - '/var/log/syslog'
    log_type: 'syslog-beat'

filebeat::outputs:
  logstash:
    hosts:
      - '172.16.1.25:5044'
