classes:
  - role_elasticsearch
  - role_logstash
  - kibana

role_elasticsearch::clustername:
  'cluster-filebeat'

role_logstash::configfile:
  'logstash-filebeat-01.conf'

kibana::kibana_password:
  'password'
kibana::kibana_hash:
  '':
    host: 'localhost'
